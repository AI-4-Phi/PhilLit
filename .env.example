# Philosophy Research API Keys
# To set a key here, uncomment its line and add your value after the "=".
# Keys exported in your shell environment work as-is and can stay commented;
# a value set in this file takes priority over the environment.

# Semantic Scholar API key (optional but recommended for higher rate limits)
# Get one at: https://www.semanticscholar.org/product/api
# S2_API_KEY=

# Email for CrossRef API (required for paper verification)
# Used to identify your requests per CrossRef's polite pool
# CROSSREF_MAILTO=

# Email for OpenAlex API (recommended for higher rate limits)
# Used to access the polite pool with faster responses
# OPENALEX_EMAIL=

# OpenAlex API key - OPTIONAL, and most users never need one.
# OpenAlex began metering by daily spend in 2026. Without a key you get
# $0.10/day, and one literature review costs roughly a quarter to a half of
# that, so two to four reviews a day work fine unkeyed. Only get a key if you
# run more than that in a day: it is free, takes ~30 seconds at
# https://openalex.org/settings/api, and raises the budget 10x while making
# DOI lookups unmetered. If you do hit the ceiling PhilLit says so explicitly
# and keeps going without OpenAlex - it does not fail the run.
# OPENALEX_API_KEY=

# Venue vetting switch - OPTIONAL. Unset: the venue-visibility check runs
# exactly when OPENALEX_API_KEY is set (the check needs the keyed budget).
# Accepted values: 1/true/yes/on (run - still requires the key) or
# 0/false/no/off (never run - keeps a key for the 10x search budget WITHOUT
# venue vetting). Any other value is reported as unrecognized and the check
# skips.
# PHILLIT_VET_VENUES=

# Brave Search API key (required for SEP/PhilPapers discovery)
# Get one at: https://brave.com/search/api/
# BRAVE_API_KEY=

# CORE API key (optional; improves CORE full-text discovery)
# Get one at: https://core.ac.uk/services/api
# CORE_API_KEY=

# Advanced: override the User-Agent sent when fetching SEP/IEP/NDPR pages
# directly. Leave unset to use PhilLit's honest default bot UA; set only if a
# downstream packaging must identify itself distinctly (e.g. with a mailto).
# PHILLIT_FETCH_USER_AGENT=
